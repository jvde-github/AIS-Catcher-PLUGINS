// {"author":"jvde-github","version": 2, "description":"filter out ships that are too far away"}

function includeShip(ship) {

        if(ship.hasOwnProperty('distance') && ship.distance && ship.distance > 1000) return false;
        return true;
}
